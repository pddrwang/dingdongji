# V2.4 本地审计交付

日期：2026-09-16。状态：待用户审计，未打包、未发布。

## 本轮范围

整理既有实现、重写 Marketplace 详情页、统一版本元数据、核对旧版安装资产。本轮没有重新验收全部后端或真实科研链路。

设计依据 design-taste-frontend：先审计现有品牌与功能，再按研究者的阅读顺序组织页面。保留小鸡品牌及现有克制科幻界面，使用静态 Markdown 和实际组件截图，不虚构使用量、成功率或科研成果。展示图为合成数据，未连接模型。

## 功能索引与验证边界

| 能力 | 主要源码入口 | 当前证据与限制 |
| --- | --- | --- |
| 科研闭环与仪表盘 | src/webview/main.js、src/researchAgent.ts、src/pipeline.ts | 已有实现；本轮未重新运行真实后端全流程 |
| 融合界面与模式切换 | src/dashboard.ts、src/webview/fusion.js、fusion.css | 实际组件渲染通过；宿主窗口交互仍需人工检查 |
| 项目内多智能体接续 | src/agentSession.ts、codexSession.ts、deepseekSession.ts | 本轮项目会话模拟回归通过；不是并行调度证明 |
| 每轮总结与工作细节 | src/agentSession.ts、src/webview/fusion.js | 本轮报告提取、去重、失败恢复模拟测试通过 |
| Codex 原生压缩 | src/codexSession.ts | 既有协议模拟测试；真实模型压缩仍待验收 |
| 作者库与当前项目手稿 | src/manuscripts.ts | 本轮范围、路径穿越、符号链接、作者结构测试通过 |
| 引用跨库核验 | biomed_bus/citation_verification.py、dashboard_server.py、src/dashboard.ts、src/extension.ts、src/webview/main.js | 后端 16 项引用测试 + 真实 DOI 双库实测（1 条通过、1 条因 Erratum 待审）；webview 卡片回归通过；真实项目全量引用仍待人工验收 |
| 对话内交互卡片 | src/extension.ts、src/webview/fusion.js、fusion.css | Chromium 真实组件回归通过：Codex 提问（选项/多选/自定义/跳过/提交/取消/越界忽略）与 DSH 审批（工具详情/选项/警示色/optionId 白名单/取消）；含 700px 短窗口“可见且可命中”断言；真实 Codex 与 Harness 端到端仍待实机验收 |
| 会话持久化与缓存 | src/sessionCache.ts、src/agentSession.ts | 既有缓存测试；跨版本迁移与异常关机仍需实机验收 |

以上“通过”仅覆盖相应测试断言，不等于无缺陷或全平台认证。

## 本轮核验

- npm run verify：通过，TypeScript 编译及三个前端脚本语法检查完成。
- vsce ls --no-dependencies：通过，仅枚举候选文件，没有生成 VSIX。
- test_project_agents.cjs：通过，迁移、双向交接、来源标记、忙碌保护、持久化和项目隔离。
- test_turn_reports.cjs：通过，报告提取、完成事件去重、原始日志保留及失败/中断恢复。
- test_manuscripts.cjs：通过，手稿范围、越界和符号链接拦截、作者结构与写入块。
- render_v24_details.py：通过，真实前端组件与合成数据渲染，无页面脚本错误；截图已目视检查。
- 引用核验后续迭代（2026-09-16）：biomed_bus 测试 73 项通过（新增引用解析/核验 11 项、接口 5 项）；`test_citation_panel.py` 真实 webview 回归通过；真实 DOI 实测两库一致判定 verified，带 Erratum 的论文判定 needs_review（fail-closed）。核验只发送公开引用元数据到固定 Crossref/NCBI 端点。
- 对话内提问组件（2026-09-16）：`test_ask_user_component.py` 真实 Chromium 回归通过，覆盖选项点选、多选、自定义输入、跳过、提交答案结构、整次取消与非当前工作区忽略；未在真实 Codex 会话中触发一次 `requestUserInput`，真实端到端仍待验收。
- DSH 审批卡片（2026-09-16）：`test_dsh_approval_card.py` 真实 Chromium 回归通过，覆盖工具详情、全部选项、`allow_always` 警示样式、回传 `optionId`、取消与非当前工作区忽略；扩展侧对 `optionId` 做选项白名单校验。未在真实 Harness 会话中触发一次 `session/request_permission`，真实端到端仍待验收。
- 交互卡片可点击性修复（2026-09-16）：真实 HTML/CSP 下复现出卡片操作按钮被 `#fusion-chat` 的 `overflow:hidden` 裁剪、无法点击；改为 flex 列 + 可滚动主体 + 固定操作区，并在卡片出现时收起管理台、允许对话区滚动。两个卡片测试在 700px 短窗口下新增 `elementFromPoint` 命中断言并通过；既有回归全部通过。未在真实 VS Code 宿主中人工点击验收。
- npm 存在用户级 verify-deps-before-run 配置警告，本轮未修改用户 npm 配置。

测试与渲染脚本在维护工作区的工程文件目录，不作为产品运行依赖。

## 上架前最终清理（2026-09-16 续）

- 打包候选清单收敛为 40 项：`out/*.js` + `src/` 运行时资源（webview/hooks/agents/plot）+ README/CHANGELOG/LICENSE/第三方声明 + `media/` 与两张详情页截图。已从候选清单排除内部维护资料 `docs/**`、`Publishing.md`、开发期 `src/**/*.ts` 与 `tsconfig.json`。
- 个人路径清理：扩展源码中项目专名改为通用表述；CHANGELOG 中 `~/Desktop/信息中心` 改为“用户配置的默认目录”。后端 `dashboard_server.py` 的 5 处 `/Users/wangdizun/ddj/...` 改为 `~/ddj/...`；删除扩展与后端的 `__pycache__` 字节码缓存。
- 已登记密钥精确扫描（`工程文件/audit_ddj_secret_exposure.py`）：528 个文本文件未命中任何已登记密钥值或 URL 编码等价形式；密钥库权限 0600。该扫描只覆盖 backend/extension/tools 文本文件，不宣称覆盖全盘、历史备份或二进制。
- 详情页新增引用核验截图，展示图均由真实前端组件与合成数据渲染，标注“未连接模型”，不含真实文献、DOI 或用户数据。
- ~~阻塞项：repository 指向不存在的 `wangdizun/dingdongji`~~ 已解决（2026-09-16）：改为 `https://github.com/pddrwang/dingdongji` 并推送；仓库为 public、默认分支 `main`，README 引用的图片/文档经公网核验均 HTTP 200。
- 本轮未安装替换、未发布。

## 打包与隔离安装验证（2026-09-16 续）

- 用户指示进入上架准备后，生成候选包 `dist/dingdongji-2.4.0.vsix`：`vsce package` 成功，42 个条目、约 707 KB；包内无 `docs/`、`Publishing.md`、`*.ts`、`tsconfig`、`.map`、`__pycache__`、`node_modules`。
- 隔离安装验证（不触碰本机正式配置）：`code --install-extension` 到 `/tmp/ddj_clean_ext` + `/tmp/ddj_clean_data` 成功；`--list-extensions` 显示 `dingdongchilk.dingdongji@2.4.0`。这只证明“可安装、清单正确”，不是完整运行时 UI 验证。
- Marketplace 查询：`DingdongChilk.dingdongji` **未发布**，2.4.0 可用；发布前需再次核对。
- `.gitignore` 增补 `node_modules/`、`out/`、`dist/`，避免把依赖与构建产物提交到公开仓库。
- 公开仓库阻塞项仍未解决（需用户创建并推送）；本地未执行 `git init`/推送。

## 发布前必须人工确认

- [ ] 在 VS Code 宿主中检查详情页、浅色/深色主题、缩放、小窗口、键盘可达性。
- [ ] 使用两个独立项目检查切换、关闭重开、删除会话、导出及恢复，确认不会串入另一项目内容。
- [ ] 真实 Codex、Harness 模型各完成一轮任务；检查工具授权、失败、中断和再次连接。
- [ ] 验证真实 Codex 压缩及额度显示；不把服务限额误称为账户余额。
- [ ] 确认智能体切换的数据告知、附件支持范围及过长历史拒绝提示。
- [ ] 在干净环境审查启动时的后端启动、上下文同步、hook/skill 配置写入以及工作区信任行为。
- [ ] 多窗口同时连接后端时验证工作区绑定，避免全局后端状态影响项目归属。
- [ ] 用无敏感数据的真实项目完成 Idea、问题集群、计划执行、证据结论与手稿全过程。
- [ ] 检查 PDF、Word 工具缺失与解析失败提示，补充 Windows、Linux 验证。
- [ ] 确认检索、模型调用的数据政策与权限；检查候选文件内容中的密钥、用户数据及个人路径。
- [ ] 确认 Marketplace 已用版本、仓库图片链接和许可证后，再决定发布版本号。
- [ ] 用户明确批准打包及后续发布。

## 版本与清理

package.json 与 package-lock.json 根元数据统一为 2.4.0，显示名称改为 V2.4。历史 CHANGELOG 原文保留，新内容以 Unreleased 标识，不追溯改写旧发布记录。

已检查 ~/.vscode/extensions、~/ddj、Downloads、Code/CachedExtensionVSIXs 的相关候选位置，未发现独立的旧版叮咚鸡安装包或安装副本。检查为限定目录范围，不宣称扫描了整个磁盘。

唯一已发现安装项 ~/.vscode/extensions/DingdongChilk.dingdongji-2.4.0 是指向当前源码的符号链接，已保留。没有删除源码、会话、项目数据、历史文档、其他扩展或远端 Marketplace 版本。

## 审阅入口

- [新版详情页](../README.md)
- [阶段更新日志](../CHANGELOG.md)
- [发布门禁](../Publishing.md)

详情页资源遵循 [VS Code 扩展发布规则](https://code.visualstudio.com/api/working-with-extensions/publishing-extension)，元数据参考 [扩展清单说明](https://code.visualstudio.com/api/references/extension-manifest)。
